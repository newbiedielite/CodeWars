# Наведено хеш md5 п’ятизначного PIN-коду. Задається як рядок. Md5 — це функція для хешування вашого пароля: "password123" ===> "482c811da5d5b4bc6d497ffa98491e38"
# Ваше завдання — повернути зламаний PIN-код у вигляді рядка.
# Почніть кодувати shell script
# Ви не повинні нічого повертати, ви повинні повторити рішення
# Хеш в $1
# Приклад: echo "$result"

# Це скрипт для брутфорс атаки на MD5 хеш:
# 1. Генеруємо всі можливі п'ятизначні PIN-коди (від "00000" до "99999").
# 2. Обчислюємо MD5 хеш кожного PIN-коду.
# 3. Порівнюємо обчислений хеш із заданим.
# 4. Якщо знайдено збіг, виводимо відповідний PIN-код.

#!/bin/bash

# Заданий MD5 хеш
target_hash="482c811da5d5b4bc6d497ffa98491e38"

# Цикл по всім можливим п'ятизначним PIN-кодам
for i in {00000..99999}; do
	# Обчислення MD5 хешу для поточного PIN-коду
	current_hash=$(echo -n "$i" | md5sum | awk '{print $1}')
	
	# Порівняння поточного хешу ші заданим хешем
	if [ "$current_hash" == "$target_hash" ]; then
		echo "Зламаний PIN-код: $1"
		break
	if

done

# 1. target_hash="482c811da5d5b4bc6d497ffa98491e38": Це хеш, який ми повинні зламати.
# 2. for i in {00000..99999}; do: Цей цикл перебирає всі можливі п'ятизначні комбінації. Послідовність {00000..99999} генерує числа від "00000" до "99999".
# 3. echo -n "$i" | md5sum | awk '{print $1}': Цей рядок обчислює MD5 хеш для поточного PIN-коду ($i). echo -n використовується, щоб уникнути додавання нового рядка, md5sum генерує хеш, а awk '{print $1}' витягує хеш з виводу.
# 4. if [ "$current_hash" == "$target_hash" ]; then: Це перевіряє, чи співпадає обчислений хеш із заданим хешем.
# 5. echo "Зламаний PIN-код: $i": Якщо хеші збігаються, виводимо правильний PIN-код.
# 6. break: Як тільки знайдемо правильний PIN-код, цикл завершується.
		
# Як запустити:
# 1. Збережіть скрипт у файл під назвою crack_pin.sh.
# 2. Зробіть його виконуваним за допомогою команди chmod +x crack_pin.sh.
# 3. Запустіть скрипт ./crack_pin.sh.

# Цей скрипт виведе правильний PIN-код, що відповідає заданому MD5 хешу.
# Зверніть увагу: виконання цього скрипту може зайняти деякий час, залежно від продуктивності системи, оскільки він перевіряє всі 100 000 можливих комбінацій.

# Ось як можна написати Shell скрипт відповідно до наших вимог:
		
#!/bin/bash
		
# Отримуємо хеш з першого аргументу комадного рядка
target_hash="$1"

# Цикл по всім можливим п'ятизначним PIN-кодам
for i in {00000..99999}; do
	# Обчислення MD5 хешу для поточного PIN-коду
	current_hash=$(echo -n "$i" | md5sum | awk '{print $1}')
	
	# Порівняння поточного хешу із заданим хешем
	if [ "$current_hash" == "$target_hash" ]; then
		echo "$i" # Виводимо знайдений PIN-код
		break
	fi
done

# Як використовувати:
# Збережіть цей код у файл, наприклад, crack_pin.sh.
# Зробіть його виконуваним: chmod +x crack_pin.sh.
# Запустіть скрипт, передавши MD5 хеш як аргумент: ./crack_pin.sh "482c811da5d5b4bc6d497ffa98491e38"
		
# Цей скрипт знайде PIN-код, відповідний заданому MD5 хешу, і виведе його на екран.